- name: alpine
  image: alpine:latest
  essential: true
  logConfiguration:
    logDriver: awslogs
    options:
      awslogs-group: /ecs-scheduled-tasks/example
      awslogs-stream-prefix: batch
      awslogs-region: ap-northeast-1
  command:
    - /bin/date
